spring:
  boot:
    admin:
      client:
         url: http://localhost:8080
server:
  port: 80

management:
  endpoint:
    health:
      show-details: always
    info:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    author: itheima
